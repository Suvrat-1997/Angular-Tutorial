import { CutomPipePipe } from './cutom-pipe.pipe';

describe('CutomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CutomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
